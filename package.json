{
  "name": "redbridge-exam-platform-netlify-supabase",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {"node": ">=20"},
  "scripts": {
    "dev": "netlify dev",
    "check": "node --check netlify/functions/api.mjs && node --check netlify/functions/pilot.mjs"
  },
  "dependencies": {
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "netlify-cli": "^23.5.0"
  }
}